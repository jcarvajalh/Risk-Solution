# Plantilla de variables de entorno. Copia este archivo a `.env` y rellena los
# valores reales. NUNCA subas `.env` al repositorio (sección 7 del CLAUDE.md).
#
# Solo las variables con prefijo PUBLIC_ se exponen al cliente (son necesarias
# para EmailJS y reCAPTCHA, que corren en el navegador sin backend propio).

# CMS de Risk (API del blog). Sin prefijo PUBLIC_: solo se usa en build/servidor
# (el custom loader de la colección `blog`). En local apunta al `pnpm dev` del CMS.
CMS_API_URL=http://localhost:3000

# FormSubmit.co — destino del formulario de contacto. El correo va en la URL del
# endpoint AJAX (es visible en la petición del navegador, no es un secreto).
# OJO: el primer envío a un correo nuevo dispara un correo de activación de
# FormSubmit; hay que confirmarlo una vez para que empiecen a llegar los mensajes.
PUBLIC_FORMSUBMIT_EMAIL=juan.carvajal03@outlook.com

# EmailJS — formulario de contacto
PUBLIC_EMAILJS_SERVICE_ID=
PUBLIC_EMAILJS_TEMPLATE_ID=
PUBLIC_EMAILJS_PUBLIC_KEY=

# Google reCAPTCHA v2 "Casilla invisible" (Site Key aquí; el Secret Key se guarda
# en el panel de EmailJS, nunca en el repo).
PUBLIC_RECAPTCHA_SITE_KEY=

# Google Tag Manager (GA4)
PUBLIC_GTM_ID=
